# Script representing the resentation layer
# It is important that this file has the same name as the class created in the logic layer file
# without the final App. This is done in order to allow kivy to find the layer file
<Label>:
    text: "Hello Kivy"
